import React from 'react'

//This is as useless as nothing.... just using it as a fallback function to my messageSubmit button
const Messages = (message) => {
    const messages = []
    messages.push(message)
    return messages
}

export default Messages